// History area routes. `HistoryTab` (screens/history/HistoryTab.jsx) is a
// tab-root component exported by name — it is intentionally NOT a route.
import { TransactionDetailScreen } from "./DetailScreen.jsx";

export const historyRoutes = {
  "history/detail": TransactionDetailScreen
};
