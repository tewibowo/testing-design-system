// v2 Activity routes — transaction detail opens as a bottom sheet
// (brief §2.5.5), not a push, so this area registers no stack screens.
// File kept per the v2 builder contract (BUILDERS-V2.md, registration).
export const v2ActivityRoutes = {};
